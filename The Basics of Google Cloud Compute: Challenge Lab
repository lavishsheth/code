gcloud compute disks create mydisk --size=200GB \
--zone ZONE_NAME

gcloud compute instances attach-disk VM_NAME --disk mydisk --zone ZONE_NAME

Now open the VM SSH, and run all the commands below and you are done with the lab

sudo apt-get update

sudo apt-get install -y nginx

ps auwx | grep nginx
